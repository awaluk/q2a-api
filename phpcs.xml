<?xml version="1.0"?>
<ruleset>
    <file>src/</file>
    <rule ref="PSR12"/>

    <!-- for PHP 7.0 we can't declare const visibility -->
    <rule ref="PSR12.Properties.ConstantVisibility">
        <exclude-pattern>/</exclude-pattern>
    </rule>
</ruleset>
